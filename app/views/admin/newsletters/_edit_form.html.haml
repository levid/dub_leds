= form_for @newsletter, :url => admin_newsletter_path(:id => @newsletter.id), :method => 'put', :html => {:multipart => true} do |f|
  / - if @newsletter.errors.any?
  /     #error_explanation
  /       %h2
  /         = pluralize(@newsletter.errors.count, "error")
  /         prohibited this newsletter from being saved:
  /       %ul
  /         - @newsletter.errors.full_messages.each do |msg|
  /           %li= msg

  .field
    = f.label :email
    %br/
    = f.text_field :email
  .actions
    = f.submit 'Save'
