.title Editing Newsletter Signup
.text-container
  %fieldset
    %legend Signup Information
    = render 'edit_form'

  %fieldset
    %legend Other options
    .links
      = link_to 'Cancel', admin_newsletter_path(@newsletter), :class => 'btn blue'
      = link_to 'Back to list', admin_newsletters_path, :class => 'btn black pad left5'
