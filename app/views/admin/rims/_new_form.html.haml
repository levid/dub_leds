= form_for @rim, :url => admin_rims_path(:id => @rim.id), :html => {:multipart => true} do |f|
  -# -if @rim.errors.any?
  -#     #error_explanation
  -#       %h2= "#{pluralize(@rim.errors.count, "error")} prohibited this rim from being saved:"
  -#       %ul
  -#         - @rim.errors.full_messages.each do |msg|
  -#           %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :description
    = f.text_area :description
  .field
    = f.label :color
    = f.text_field :color
  .field
    = f.label :size
    = f.text_field :size
  .field
    = f.label :fitting
    = f.text_field :fitting
  .field
    .border.pad.bottom10
      = f.label :image
      = f.file_field :image
  .field
    .border.pad.bottom10
      = f.label :medium_image
      = f.file_field :medium_image
  .field
    .border.pad.bottom10
      = f.label :small_image
      = f.file_field :small_image
  .submit
    = f.submit 'Save', :class => 'orange', :title => "Creating Rim..."
