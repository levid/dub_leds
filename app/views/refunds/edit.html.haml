.title Editing refund
.text-container
  %fieldset
    %legend Policy Information
    = render 'form'

  %fieldset
    %legend Other options
    .links
      = link_to 'Cancel', @refund, :class => 'btn blue'
      = link_to 'Back to list', refunds_path, :class => 'btn black pad left5'
