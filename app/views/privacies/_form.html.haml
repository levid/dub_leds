= form_for(@privacy) do |f|
  - if @privacy.errors.any?
    #error_explanation
      %h2
        = pluralize(@privacy.errors.count, "error")
        prohibited this privacy from being saved:
      %ul
        - @privacy.errors.full_messages.each do |msg|
          %li= msg
  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :content
    = f.text_area :content
  .submit
    = f.submit "Submit", :class => 'orange'
